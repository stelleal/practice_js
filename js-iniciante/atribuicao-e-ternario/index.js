// var paginasLidas = 30;
// paginasLidas += 30;

// var metaLeitura = paginasLidas >= 50 ? 'Boa leitura.' : 'Leia um pouco mais.';

// console.log(metaLeitura);

// var possuiGatos = true;

// if (possuiGatos) console.log('Possui gatos.');
// else console.log('Não possui gatos.');

// Some 500 ao valor de scroll abaixo,
// atribuindo o novo valor a scroll
var scroll = 1000;
scroll += 500;
console.log(scroll);

// Atribua true para a variável darCredito,
// caso o cliente possua carro e casa.
// E false caso o contrário.
var possuiCarro = true;
var possuiCasa = true;
var darCredito;

darCredito =
	possuiCarro && possuiCasa ? 'Crédito aprovado.' : 'Crédito não aprovado.';

console.log(darCredito);
